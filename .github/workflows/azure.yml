name: Build and deploy Node.js app to Azure Web App

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: control-canastillos    # CAMBIAR a tu nombre de App Service
  NODE_VERSION: '20.x'                      # Versión de Node compatible

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: npm install, build, and prep standalone
        run: |
          npm ci
          npm run build
          # Preparar la carpeta para el despliegue standalone a Azure
          cp -r public .next/standalone/
          cp -r .next/static .next/standalone/.next/
          # zip the standalone app to speed up deployment artifact upload
          cd .next/standalone
          zip -qq -r ../../nextjs-app.zip .

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: nextjs-app.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: node-app

      - name: Unzip artifact
        run: unzip -qq nextjs-app.zip -d ./app

      # Reemplaza 'PUBLISH_PROFILE_SECRET' por el nombre del secreto que agregaste en GitHub
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ./app
